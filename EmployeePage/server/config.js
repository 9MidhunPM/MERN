// Configuration for data source
module.exports = {
  // Set to 'custom' to use local sample data, 'external' to use jsonplaceholder API
  dataSource: 'custom', // Options: 'custom' or 'external'
  
  // External API URL (used when dataSource is 'external')
  externalApiUrl: 'https://jsonplaceholder.typicode.com/users'
};

